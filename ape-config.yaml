name: yeth-periphery
plugins:
  - name: vyper
  - name: foundry
  - name: etherscan
ethereum:
  default_network: mainnet-fork
  local:
    default_provider: foundry
  mainnet_fork:
    default_provider: foundry
    transaction_acceptance_timeout: 600
vyper:
  evm_version: cancun
foundry:
  evm_version: cancun
